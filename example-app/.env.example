# Required only for hosted Plutus compilation. Leave empty for Aiken-only use.
KUBERIDE_API_KEY=

# Optional compiler endpoint and address-network overrides.
AIKEN_COMPILER_URL=
COMPILER_URL=
MESH_NETWORK_ID=0
