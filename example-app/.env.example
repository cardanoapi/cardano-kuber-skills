# Required only for hosted Plutus compilation. Leave empty for Aiken-only use.
KUBERIDE_API_KEY=

# Both compiler URLs are required. Keep their real values only in .env.
AIKEN_COMPILER_URL=
COMPILER_URL=
MESH_NETWORK_ID=0
