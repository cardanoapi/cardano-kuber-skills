{
  "name": "cardano-kuber-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.1.0",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "scripts": {
    "build": "pnpm --dir plugins/cardano-kuber build",
    "test": "pnpm --dir plugins/cardano-kuber test && node --test 'tests/*.test.mjs'",
    "typecheck": "pnpm --dir plugins/cardano-kuber typecheck",
    "check": "pnpm --dir plugins/cardano-kuber check && node --test 'tests/*.test.mjs'"
  }
}
